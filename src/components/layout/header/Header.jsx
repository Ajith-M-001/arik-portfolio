import React from "react";
import styles from "./header.module.css";

const Header = () => {
  return <header className={`u-container  ${styles.header}`}>Header</header>;
};

export default Header;
